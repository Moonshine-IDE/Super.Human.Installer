# Plymouth configuration for dual console setup
[Daemon]
Theme=startcloud
ShowDelay=0
DeviceTimeout=8
# Ensure Plymouth only shows on the framebuffer console
DeviceScale=1
# Force Plymouth to use framebuffer device
TTY=/dev/tty0
